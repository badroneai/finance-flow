# SPR-008 — تقرير إصلاح الرسائل المتناقضة + Onboarding + WelcomeBanner

**التاريخ:** 2026-03-18
**النتيجة:** ✅ نجاح — 4 ملفات مُعدّلة، Build ✅، Tests 5/5 ✅

---

## الملفات المُعدّلة

### 1. `src/ui/OnboardingModal.jsx`

| البند | القديم | الجديد |
|-------|--------|--------|
| استيراد | بدون DataContext | `import { useData }` → `isCloudMode` |
| الخطوة 1 | `بياناتك تُحفظ محليًا داخل المتصفح` | **سحابي:** `بياناتك محفوظة بأمان في السحابة ومتاحة من أي جهاز` / **محلي:** `بياناتك تُحفظ على هذا الجهاز فقط — ننصح بعمل نسخ احتياطي دوري` |
| الخطوة 2 | `ابدأ بإضافة أول حركة مالية من زر (إضافة)` | `أنشئ أول دفتر (عقار) — لتجميع حركاتك المالية في مكان واحد` |
| الخطوة 3 | `يمكنك التصدير CSV في أي وقت` | `أضف التزاماتك المتكررة (إيجار، صيانة، كهرباء...)` |
| خطوة جديدة 4 | — | `راقب صحتك المالية من النبض` |
| القائمة | `<ul>` (غير مرقّمة) | `<ol>` (مرقّمة — خطوات متسلسلة) |
| الزر الرئيسي | `ابدأ الآن` → `onClose()` | `أنشئ أول دفتر` → `window.location.hash = '#/ledgers'` |

### 2. `src/ui/WelcomeBanner.jsx`

| البند | القديم | الجديد |
|-------|--------|--------|
| مصدر البيانات | `storageFacade.getRaw` + `safeGet(KEYS.transactions)` — localStorage مباشرة | `useData()` → `transactions.length` + `commissions.length` |
| التوافق السحابي | لا يعمل (يقرأ localStorage فقط) | يعمل في الوضعين (DataContext يوفّر البيانات) |
| التفاعلية | `useEffect([], [])` — يُحسب مرة واحدة عند التحميل | `useEffect([transactions.length, commissions.length])` — يُحدّث عند تغيّر البيانات |
| النص | `ابدأ بإضافة عقار` | `ابدأ بإنشاء دفتر (عقار)` |
| حالة الرؤية | localStorage فقط (مقبول — حالة UI محلية) | بدون تغيير — localStorage لحفظ حالة UI |

### 3. `src/pages/SettingsPage.jsx`

| البند | القديم | الجديد |
|-------|--------|--------|
| قسم المزامنة (سحابي) | `المزامنة السحابية (Pro) — قريبًا` + شارة `Pro` | `بياناتك متزامنة مع السحابة` + شارة `مُفعّل` + خلفية خضراء |
| قسم المزامنة (محلي) | نفس النص أعلاه (ثابت) | `المزامنة السحابية` + شارة `غير مُفعّل` + `لتفعيل المزامنة... سجّل حساباً` |
| المنطق | نص ثابت بغض النظر عن الوضع | `isCloudMode ? ... : ...` — ديناميكي |

### 4. `src/ui/HelpPanel.jsx`

| البند | القديم | الجديد |
|-------|--------|--------|
| العنوان | `المساعدة (Help / FAQ)` | `الأسئلة الشائعة والمساعدة` |
| الالتزامات — السطر 2 | `Inbox يساعدك في:` | `المستحقات تساعدك في متابعة:` |
| الالتزامات — السطر 3 | `ينشئ حركة (category=other)` | `ينشئ حركة مالية (تصنيف: أخرى)` |
| الخصوصية (سحابي) | `بياناتك تُحفظ محليًا داخل المتصفح...` (خطأ!) | `بياناتك محفوظة بأمان في السحابة ومشفّرة. لا يمكن لأحد غيرك الوصول إليها.` |
| الخصوصية (محلي) | نفس النص | `بياناتك تُحفظ على هذا الجهاز فقط داخل المتصفح. لا يوجد رفع تلقائي للسحابة.` |
| كيف أبدأ — الخطوة 1 | `اذهب إلى: الحركات المالية → اضغط (إضافة)` | `اذهب إلى: الدفاتر → أنشئ أول دفتر (عقار) وعيّنه كنشط` (الدفتر أولاً) |
| كيف أبدأ — الخطوة 2 | `اذهب إلى: الدفاتر → عيّن دفتر كنشط` | `اذهب إلى: الحركات المالية → اضغط (إضافة) وسجّل أول حركة` |
| استيراد | بدون DataContext | `import { useData }` → `isCloudMode` |

---

## سيناريوهات التحقق

| السيناريو | النتيجة |
|-----------|---------|
| وضع سحابي: OnboardingModal يقول "بياناتك في السحابة" | ✅ |
| وضع محلي: OnboardingModal يقول "بياناتك محلية" | ✅ |
| OnboardingModal: 4 خطوات (دفتر → حركة → التزامات → نبض) | ✅ |
| الزر يوجّه لـ `#/ledgers` | ✅ |
| SettingsPage سحابي: "متزامنة مع السحابة" + شارة خضراء | ✅ |
| SettingsPage محلي: "لتفعيل... سجّل حساباً" | ✅ |
| HelpPanel: لا نصوص إنجليزية | ✅ |
| HelpPanel: خصوصية ديناميكية | ✅ |
| WelcomeBanner: يختفي إذا عند المستخدم بيانات | ✅ |
| WelcomeBanner: يعمل في السحابي | ✅ |
| `npm run build` | ✅ (2.68 ثانية) |
| `npm test` | ✅ 5/5 |

---

## ملاحظات

- جميع المكونات تستخدم `try/catch` حول `useData()` للتعامل مع حالة الاستخدام خارج `DataProvider` (مثلاً في اختبارات معزولة).
- حالة "تم رؤية البانر" (`STORAGE_KEYS.UI_WELCOME`) تبقى في localStorage — وهذا مقبول لأنها حالة UI محلية وليست بيانات عمل.
- لم يُغيّر أي منطق عمل — فقط رسائل ونصوص.

---

*نهاية التقرير — SPR-008 UX*
