# التعليمات للمساعد

اكتب هنا التعليمات أو المتطلبات كاملة، ثم احفظ الملف وأخبر المساعد لقراءته والتنفيذ.

---

## التعليمات

(اكتب تعليماتك أدنا# 🏗️ برومبتات إعادة تغليف الدفاتر → النبض المالي
## مشروع قيد العقار — "افتح قيد كل صباح — واعرف صحة مكتبك المالية بثانية واحدة"

---

## 📋 فهرس المراحل

| المرحلة | الوصف | عدد البرومبتات |
|---------|-------|----------------|
| **0** | التحضير وإعادة الهيكلة | 4 برومبتات |
| **1** | النبض المالي (الشاشة الرئيسية) | 5 برومبتات |
| **2** | صندوق الوارد المالي (المستحقات) | 3 برومبتات |
| **3** | التنبيهات الاستباقية | 3 برومبتات |
| **4** | مقارنة الدفاتر | 2 برومبت |
| **5** | التقرير الشهري PDF | 2 برومبت |
| **6** | مؤشر صحة المكتب | 3 برومبتات |
| **7** | التنظيف النهائي والتلميع | 2 برومبت |

---

## ⚙️ تعليمات الاستخدام

### قبل كل برومبت:
1. تأكد أنك في مجلد المشروع `finance-flow`
2. أرفق الملفات المشار إليها في كل برومبت
3. نفّذ البرومبتات **بالترتيب** — كل برومبت يبني على سابقه
4. بعد كل برومبت: اختبر → تأكد → ثم انتقل للتالي

### الملفات المرجعية الأساسية (أرفقها دائمًا):
- `src/core/ledger-store.js`
- `src/domain/ledgers.js`
- `src/core/dataStore.js`
- `src/core/storage-adapter.js`

---

## المرحلة 0: التحضير وإعادة الهيكلة

### برومبت 0.1 — تدقيق الوضع الحالي وإنشاء خريطة التبعيات

```
أنت مهندس برمجيات أول متخصص في إعادة هيكلة الكود (Refactoring Architect).

المشروع: قيد العقار (finance-flow) — نظام إدارة التدفق النقدي للمكاتب العقارية الصغيرة.

المطلوب:
1. افحص جميع الملفات التالية وأنشئ خريطة تبعيات (dependency map) توضح:
   - أي ملف يستورد من أي ملف
   - أي دالة تُستخدم في أي مكان
   - أي حدث (event) يُطلق ويُستقبل أين

الملفات المطلوب فحصها:
- src/core/ledger-store.js
- src/core/ledger-reports.js
- src/core/ledger-budgets.js
- src/core/ledger-compliance.js
- src/core/ledger-inbox.js
- src/core/ledger-cash-plan.js
- src/core/ledger-item-history.js
- src/core/ledger-budget-authority.js
- src/core/ledger-forecast.js
- src/core/ledger-variance.js
- src/core/ledger-intelligence-v1.js
- src/core/ledger-brain.js
- src/core/dataStore.js
- src/domain/ledgers.js
- src/domain/ledgerTemplates.js
- src/pages/LedgersPage.jsx
- src/pages/TransactionsPage.jsx
- src/ui/ledger/LedgerHeader.jsx
- src/ui/ledger/LedgerTabsShell.jsx
- src/tabs/LedgerRecurringTab.jsx
- src/tabs/LedgerReportsTab.jsx
- src/tabs/LedgerPerformanceTab.jsx

2. أنشئ ملف `DEPENDENCY_MAP.md` في جذر المشروع يحتوي:
   - رسم شجري للتبعيات
   - قائمة بالدوال المُصدّرة من كل ملف واستخداماتها
   - قائمة بالأحداث (events) وأماكن إطلاقها واستقبالها
   - تحديد الملفات "الميتة" (التي لا تُستخدم فعليًا)

3. أنشئ ملف `REFACTOR_PLAN.md` يحتوي خطة تبسيط تقسم الملفات إلى:
   - أساسي (يبقى كما هو أو يُدمج): ledger-store, dataStore, ledgers domain
   - يُدمج في ملف واحد: ledger-reports + ledger-budgets + ledger-variance → ledger-analytics.js
   - يُدمج في ملف واحد: ledger-compliance + ledger-intelligence-v1 + ledger-brain → ledger-health.js
   - يُدمج في ملف واحد: ledger-inbox + ledger-cash-plan + ledger-forecast → ledger-planner.js
   - يُؤجل (لا يُحذف لكن يُعزل): ledger-budget-authority, ledger-item-history

لا تعدّل أي كود الآن. فقط حلل وأنشئ الملفين.
```

---

### برومبت 0.2 — تنفيذ دمج ملفات Core

```
أنت مهندس برمجيات أول. سننفذ خطة إعادة الهيكلة من REFACTOR_PLAN.md.

السياق: مشروع قيد العقار يحتوي 11 ملف core للدفاتر. سندمجها في 3 ملفات رئيسية.

المطلوب تنفيذه بالترتيب:

1. أنشئ `src/core/ledger-analytics.js`:
   - ادمج المنطق من: ledger-reports.js + ledger-budgets.js + ledger-variance.js
   - صدّر كل الدوال العامة بنفس أسمائها الأصلية (لعدم كسر التوافق)
   - أضف تعليقات واضحة تفصل الأقسام الثلاثة

2. أنشئ `src/core/ledger-health.js`:
   - ادمج المنطق من: ledger-compliance.js + ledger-intelligence-v1.js + ledger-brain.js
   - صدّر كل الدوال العامة بنفس أسمائها

3. أنشئ `src/core/ledger-planner.js`:
   - ادمج المنطق من: ledger-inbox.js + ledger-cash-plan.js + ledger-forecast.js
   - صدّر كل الدوال العامة بنفس أسمائها

4. حدّث جميع الاستيرادات (imports) في الملفات التالية لتشير للملفات الجديدة:
   - LedgersPage.jsx
   - LedgerRecurringTab.jsx
   - LedgerReportsTab.jsx
   - LedgerPerformanceTab.jsx
   - LedgerInboxTab.jsx
   - LedgerForecastTab.jsx
   - وأي ملف آخر يستورد من الملفات القديمة

5. أبقِ الملفات القديمة مؤقتًا (لا تحذفها) لكن أضف تعليقًا في أول كل ملف:
   // @deprecated — moved to ledger-analytics.js / ledger-health.js / ledger-planner.js

6. اختبر أن التطبيق يعمل بدون أخطاء بعد الدمج.

قواعد:
- لا تغيّر أي منطق أعمال — فقط نقل وتنظيم
- حافظ على نفس أسماء الدوال المُصدّرة
- لا تكسر أي import موجود
```

---

### برومبت 0.3 — إعادة هيكلة التنقل والتبويبات

```
أنت مصمم UX ومطور React أول.

السياق: صفحة الدفاتر الحالية تحتوي 4 تبويبات (دفاتر، التزامات متكررة، تقارير، أداء).
نريد إعادة تصميم هيكل التنقل بالكامل ليتمحور حول "النبض المالي".

الهيكل الجديد المطلوب:

الشريط الجانبي أو التنقل السفلي (Mobile-first):
┌─────────────────────────────────────┐
│  🏠 النبض    (الرئيسية — جديدة)     │
│  📬 المستحقات (صندوق الوارد — جديد) │
│  📊 الدفاتر   (إدارة الدفاتر)       │
│  💰 الحركات   (صفحة الحركات الحالية) │
│  ⚙️ الإعدادات                       │
└─────────────────────────────────────┘

المطلوب:

1. أنشئ ملف `src/config/navigation.js`:
```js
export const NAV_ITEMS = [
  { id: 'pulse', label: 'النبض', icon: 'HeartPulse', path: '/', component: 'PulsePage' },
  { id: 'inbox', label: 'المستحقات', icon: 'Inbox', path: '/inbox', component: 'InboxPage' },
  { id: 'ledgers', label: 'الدفاتر', icon: 'BookOpen', path: '/ledgers', component: 'LedgersPage' },
  { id: 'transactions', label: 'الحركات', icon: 'ArrowLeftRight', path: '/transactions', component: 'TransactionsPage' },
  { id: 'settings', label: 'الإعدادات', icon: 'Settings', path: '/settings', component: 'SettingsPage' },
];
```

2. عدّل `src/main.jsx` أو ملف التوجيه (Router) ليعكس المسارات الجديدة:
   - `/` → PulsePage (جديدة — ستُبنى لاحقًا، حاليًا placeholder)
   - `/inbox` → InboxPage (جديدة — placeholder)
   - `/ledgers` → LedgersPage (الحالية)
   - `/transactions` → TransactionsPage (الحالية)
   - `/settings` → SettingsPage (الحالية أو placeholder)

3. أنشئ `src/pages/PulsePage.jsx` كـ placeholder:
```jsx
export default function PulsePage() {
  return (
    <div className="pulse-page" dir="rtl">
      <h1>النبض المالي</h1>
      <p>قريبًا — اعرف صحة مكتبك المالية بثانية واحدة</p>
    </div>
  );
}
```

4. أنشئ `src/pages/InboxPage.jsx` كـ placeholder مشابه.

5. عدّل مكون التنقل الحالي (Sidebar أو BottomNav) ليستخدم NAV_ITEMS.

6. اجعل الصفحة الافتراضية (الرئيسية) هي PulsePage بدلاً من أي صفحة أخرى.

قواعد:
- لا تحذف أي صفحة موجودة
- حافظ على RTL في كل المكونات الجديدة
- استخدم lucide-react للأيقونات
- التصميم Mobile-first: شريط تنقل سفلي للموبايل، جانبي للديسكتوب
```

---

### برومبت 0.4 — إنشاء طبقة حساب النبض (Pulse Engine)

```
أنت مهندس بيانات متخصص في الأنظمة المالية.

السياق: مشروع قيد العقار. سنبني "محرك النبض" — الطبقة الحسابية التي تغذي شاشة النبض المالي.

المطلوب: أنشئ ملف `src/core/pulse-engine.js`

هذا الملف يجب أن يحسب ويرجع كائنًا واحدًا (Pulse Object) يحتوي كل ما تحتاجه الواجهة:

```js
/**
 * pulse-engine.js
 * محرك النبض المالي — يحسب الصحة المالية للدفتر النشط
 * 
 * يعتمد على:
 * - ledger-store.js (قراءة الدفتر النشط والالتزامات)
 * - dataStore.js (قراءة الحركات)
 * - ledger-planner.js (التوقعات وصندوق الوارد)
 * - ledger-health.js (درجة الصحة والتنبيهات)
 */

export function calculatePulse(ledgerId) {
  // يرجع:
  return {
    // === القسم 1: الأرقام الثلاثة الكبرى ===
    todayIncome: Number,        // إجمالي الدخل اليوم
    weekExpenses: Number,       // إجمالي المصروفات هذا الأسبوع
    currentBalance: Number,     // الرصيد الحالي (آخر رصيد محسوب)
    balanceTrend: 'up' | 'down' | 'stable',  // مقارنة بالأسبوع الماضي
    
    // === القسم 2: حالة الصحة ===
    healthScore: Number,        // 0-100 درجة الصحة المالية
    healthStatus: 'excellent' | 'good' | 'warning' | 'critical',
    healthColor: String,        // لون CSS حسب الحالة
    
    // === القسم 3: التنبيهات العاجلة ===
    alerts: [
      {
        id: String,
        type: 'overdue' | 'upcoming' | 'anomaly' | 'cashflow_risk',
        severity: 'critical' | 'warning' | 'info',
        title: String,          // مثل: "إيجار شقة 5 متأخر 3 أيام"
        amount: Number,
        dueDate: String,        // ISO date
        actionLabel: String,    // مثل: "سجّل الدفعة"
        actionType: String,     // مثل: "record_payment"
      }
    ],
    
    // === القسم 4: نظرة الأسبوع ===
    weekForecast: {
      expectedIncome: Number,
      expectedExpenses: Number,
      netCashflow: Number,
      riskLevel: 'safe' | 'tight' | 'danger',
    },
    
    // === القسم 5: المستحقات القريبة (أقرب 5) ===
    upcomingDues: [
      {
        id: String,
        name: String,
        amount: Number,
        dueDate: String,
        daysRemaining: Number,
        type: 'income' | 'expense',
        recurring: Boolean,
      }
    ],
    
    // === القسم 6: ملخص الدفتر ===
    ledgerSummary: {
      ledgerId: String,
      ledgerName: String,
      ledgerType: String,
      totalTransactions: Number,
      activeRecurringItems: Number,
      monthlyAvgIncome: Number,
      monthlyAvgExpense: Number,
    },
    
    // === بيانات وصفية ===
    calculatedAt: String,       // ISO timestamp
    dataFreshness: 'live' | 'cached',
  };
}

// دالة مساعدة: حساب healthScore
export function calculateHealthScore(ledgerId) {
  // المعايير:
  // - نسبة الالتزامات المدفوعة في وقتها (40% من الدرجة)
  // - نسبة الدخل/المصروف (20%)
  // - عدد التنبيهات الحرجة (20%)
  // - استقرار التدفق النقدي خلال 3 أشهر (20%)
}

// دالة مساعدة: اكتشاف التنبيهات
export function detectAlerts(ledgerId) {
  // أنواع التنبيهات:
  // 1. overdue: التزامات متأخرة عن موعد الاستحقاق
  // 2. upcoming: التزامات تستحق خلال 7 أيام
  // 3. anomaly: حركة مالية غير طبيعية (أكبر من 3x المتوسط)
  // 4. cashflow_risk: توقع عجز في السيولة خلال 30 يوم
}

// دالة مساعدة: توقعات الأسبوع
export function calculateWeekForecast(ledgerId) {
  // يحسب بناء على:
  // - الالتزامات المتكررة المستحقة هذا الأسبوع
  // - متوسط الدخل اليومي من آخر 4 أسابيع
  // - الحركات المجدولة
}
```

قواعد التنفيذ:
- استخدم الدوال الموجودة فعلًا في ledger-store.js و dataStore.js — لا تعيد اختراع العجلة
- إذا وجدت دالة مشابهة في الملفات المدمجة (ledger-health, ledger-planner, ledger-analytics) استخدمها
- أضف JSDoc لكل دالة
- أضف اختبار أساسي في نهاية الملف (self-test) يمكن تشغيله مستقلًا
- التواريخ بتوقيت السعودية (Asia/Riyadh)
- العملة الافتراضية SAR
- إذا لم يكن هناك دفتر نشط، ارجع كائن pulse فارغ مع healthStatus: 'unknown'
```

---

## المرحلة 1: النبض المالي (الشاشة الرئيسية)

### برومبت 1.1 — بطاقة النبض الرئيسية (Hero Card)

```
أنت مطور React أول ومصمم UI متخصص في الواجهات المالية العربية.

السياق: نبني شاشة "النبض المالي" — الشاشة الرئيسية لتطبيق قيد العقار.
الشعار: "افتح قيد كل صباح — واعرف صحة مكتبك المالية بثانية واحدة"

لدينا `src/core/pulse-engine.js` الذي يرجع كائن pulse جاهز.

المطلوب: أنشئ `src/ui/pulse/PulseHeroCard.jsx`

التصميم:
┌─────────────────────────────────────────────┐
│                                             │
│          صحة مكتبك المالية                  │
│                                             │
│            ╭──────────╮                     │
│            │          │                     │
│            │   78     │  ← رقم كبير جدًا    │
│            │  /100    │     بلون ديناميكي    │
│            ╰──────────╯                     │
│          ● ● ● ● ● ○ ○ ○ ○ ○               │
│            حالة جيدة ↑                      │
│                                             │
│  ┌──────────┬──────────┬──────────┐         │
│  │ دخل اليوم│ مصروف    │ الرصيد   │         │
│  │          │ الأسبوع  │ الحالي   │         │
│  │ 12,500   │ 8,200    │ 45,300   │         │
│  │   ر.س    │   ر.س    │   ر.س  ↑ │         │
│  └──────────┴──────────┴──────────┘         │
│                                             │
│  آخر تحديث: اليوم 8:30 ص                   │
└─────────────────────────────────────────────┘

متطلبات التنفيذ:

1. المكون يستقبل prop واحد: `pulse` (كائن من pulse-engine)

2. دائرة الصحة (Health Circle):
   - استخدم SVG circle مع stroke-dasharray للتقدم
   - الرقم في المنتصف بخط كبير جدًا (48px+)
   - الألوان:
     * 80-100: أخضر (#10B981)
     * 60-79: أزرق (#3B82F6)
     * 40-59: برتقالي (#F59E0B)
     * 0-39: أحمر (#EF4444)
   - حركة animation عند التحميل (count-up من 0 للرقم)

3. البطاقات الثلاث (الأرقام الكبرى):
   - كل بطاقة: أيقونة صغيرة + تسمية + رقم بتنسيق عربي
   - الرصيد يحتوي سهم اتجاه (↑ أخضر أو ↓ أحمر)
   - الأرقام بتنسيق: 12,500 ر.س (toLocaleString('ar-SA'))
   - إذا الرقم = 0 يظهر "—" بدل الصفر

4. التصميم:
   - RTL بالكامل
   - خلفية البطاقة: تدرج خفيف حسب حالة الصحة
   - زوايا مدورة (rounded-2xl)
   - ظل خفيف (shadow-lg)
   - متجاوب (responsive): يتكيف مع الموبايل والديسكتوب
   - لا تستخدم أي مكتبة UI خارجية — Tailwind فقط

5. أضف حالة فارغة (Empty State):
   - إذا pulse.healthStatus === 'unknown':
     عرض رسالة "أضف أول حركة مالية لتفعيل النبض" مع زر "إضافة حركة"

6. اجعل المكون قابلًا للسحب للتحديث (pull-to-refresh ready):
   - أضف callback prop: onRefresh

لا تستخدم أي مكتبة رسوم بيانية — SVG مباشر فقط.
اكتب CSS animations مباشرة في ملف المكون (inline styles أو Tailwind).
```

---

### برومبت 1.2 — شريط التنبيهات العاجلة

```
أنت مطور React أول.

السياق: نبني شاشة النبض المالي لتطبيق قيد العقار.

المطلوب: أنشئ `src/ui/pulse/PulseAlerts.jsx`

التصميم:
┌─────────────────────────────────────────────┐
│ 🔴 تنبيهات عاجلة (3)                       │
├─────────────────────────────────────────────┤
│ ⚠️ إيجار شقة 5 متأخر 3 أيام    5,000 ر.س  │
│                          [سجّل الدفعة →]    │
├─────────────────────────────────────────────┤
│ ⏰ عمولة وسيط تستحق بعد يومين   1,200 ر.س  │
│                          [عرض التفاصيل →]   │
├─────────────────────────────────────────────┤
│ 📉 تحذير: عجز سيولة متوقع خلال 15 يوم     │
│    ستحتاج 23,000 ر.س إضافية                │
│                          [خطة السيولة →]     │
└─────────────────────────────────────────────┘

متطلبات:

1. المكون يستقبل: `alerts` (مصفوفة من pulse.alerts)

2. ترتيب التنبيهات: critical أولاً → warning → info

3. كل تنبيه:
   - أيقونة حسب severity (🔴 critical, 🟡 warning, 🔵 info)
   - عنوان + مبلغ (إن وجد)
   - زر إجراء (actionLabel) يطلق callback: onAlertAction(alert)
   - عداد الأيام: "متأخر X يوم" أو "بعد X أيام" محسوب من dueDate

4. إذا لا توجد تنبيهات: عرض رسالة "كل شيء على ما يرام 🎉"

5. حد أقصى 5 تنبيهات مع رابط "عرض الكل" إذا أكثر

6. التصميم:
   - RTL
   - كل تنبيه critical له خلفية حمراء خفيفة (bg-red-50)
   - حركة دخول (slide-in) لكل تنبيه
   - قابل للإخفاء (swipe to dismiss على الموبايل أو زر X)
```

---

### برومبت 1.3 — نظرة الأسبوع (Week Forecast)

```
أنت مطور React ومصمم بيانات مرئية.

السياق: شاشة النبض المالي — قسم "نظرة الأسبوع".

المطلوب: أنشئ `src/ui/pulse/WeekForecast.jsx`

التصميم:
┌─────────────────────────────────────────────┐
│ 📅 نظرة الأسبوع                            │
│                                             │
│  الأحد  الاثنين الثلاثاء الأربعاء الخميس   │
│   ▲       ▲                ▼               │
│   │       │                │               │
│  +2k    +5k              -8k               │
│   ░░░░░░░░░░░░░░░░████████████             │
│   ──────────────── خط الصفر ────            │
│                                             │
│  متوقع الدخل:    7,000 ر.س                 │
│  متوقع المصروف:  8,000 ر.س                 │
│  ─────────────────────                      │
│  صافي:          -1,000 ر.س ⚠️              │
│                                             │
│  [الحالة: سيولة ضيقة]                      │
└─────────────────────────────────────────────┘

متطلبات:

1. المكون يستقبل: `weekForecast` من pulse + `upcomingDues` للتفصيل اليومي

2. رسم بياني أعمدة (Bar Chart) بسيط بـ SVG:
   - أعمدة خضراء للدخل المتوقع كل يوم
   - أعمدة حمراء للمصروف المتوقع
   - خط أفقي عند الصفر
   - أيام الأسبوع بالعربي (الأحد → الخميس)
   - الجمعة والسبت بلون باهت (عطلة)

3. ملخص رقمي أسفل الرسم:
   - متوقع الدخل (أخضر)
   - متوقع المصروف (أحمر)
   - الصافي (أخضر إذا موجب، أحمر إذا سالب)

4. شارة الحالة:
   - safe → "سيولة مريحة" (أخضر)
   - tight → "سيولة ضيقة" (برتقالي)
   - danger → "خطر عجز" (أحمر مع اهتزاز خفيف)

5. لا تستخدم recharts أو أي مكتبة — SVG يدوي فقط
6. RTL + أرقام عربية
7. الرسم يجب أن يكون متجاوبًا (يتمدد مع العرض)
```

---

### برومبت 1.4 — قائمة المستحقات القريبة

```
أنت مطور React أول.

السياق: شاشة النبض — قسم "أقرب المستحقات".

المطلوب: أنشئ `src/ui/pulse/UpcomingDues.jsx`

التصميم:
┌─────────────────────────────────────────────┐
│ 📋 أقرب المستحقات                          │
│                                             │
│ ┌─ غدًا ────────────────────────────────┐   │
│ │ ↗ إيجار شقة 3          +5,000 ر.س    │   │
│ └───────────────────────────────────────┘   │
│ ┌─ بعد 3 أيام ─────────────────────────┐   │
│ │ ↙ صيانة مبنى 2         -2,300 ر.س    │   │
│ └───────────────────────────────────────┘   │
│ ┌─ بعد 5 أيام ─────────────────────────┐   │
│ │ ↗ عمولة تأجير           +800 ر.س     │   │
│ └───────────────────────────────────────┘   │
│                                             │
│         [ عرض كل المستحقات → ]              │
└─────────────────────────────────────────────┘

متطلبات:

1. يستقبل: `upcomingDues` (مصفوفة أقصاها 5)

2. كل بند:
   - أيقونة: ↗ أخضر للدخل، ↙ أحمر للمصروف
   - اسم البند
   - المبلغ بتنسيق عربي مع + أو -
   - الزمن: "اليوم" / "غدًا" / "بعد X أيام" (محسوب من daysRemaining)
   - شارة "متكرر" إذا recurring === true

3. ترتيب: الأقرب أولاً

4. زر "عرض كل المستحقات" ينتقل لصفحة /inbox

5. تصميم: بطاقات مسطحة، RTL، ألوان هادئة
```

---

### برومبت 1.5 — تجميع صفحة النبض الكاملة

```
أنت مطور React أول ومنسق واجهات.

السياق: أنشأنا المكونات التالية:
- PulseHeroCard.jsx (بطاقة النبض الرئيسية)
- PulseAlerts.jsx (التنبيهات العاجلة)
- WeekForecast.jsx (نظرة الأسبوع)
- UpcomingDues.jsx (المستحقات القريبة)

المطلوب: عدّل `src/pages/PulsePage.jsx` لتجميع كل شيء.

```jsx
// الهيكل المطلوب:
<PulsePage>
  ├── <PulseHeader>           // اسم الدفتر النشط + زر تبديل الدفتر
  ├── <PulseHeroCard>         // بطاقة النبض الكبرى
  ├── <PulseAlerts>           // التنبيهات (إذا وُجدت)
  ├── <WeekForecast>          // نظرة الأسبوع
  ├── <UpcomingDues>          // المستحقات القريبة
  └── <PulseFooter>           // "آخر تحديث" + زر "تحديث يدوي"
</PulsePage>
```

متطلبات:

1. استخدم `calculatePulse(activeLedgerId)` من pulse-engine.js لجلب البيانات

2. State management:
   - useState للـ pulse object
   - useEffect لحساب النبض عند التحميل وعند تغيير الدفتر
   - استمع لحدث 'ledger:activeChanged' لإعادة الحساب
   - loading state أثناء الحساب

3. PulseHeader:
   - يعرض اسم الدفتر النشط
   - زر تبديل الدفتر (dropdown سريع)
   - شعار "قيد" صغير

4. التحديث:
   - تحديث تلقائي كل 5 دقائق
   - زر تحديث يدوي
   - Pull-to-refresh على الموبايل

5. الحالات:
   - Loading: skeleton screens (ليس spinner)
   - Empty: لا يوجد دفتر → "أنشئ أول دفتر"
   - Empty: دفتر بدون حركات → "أضف أول حركة"
   - Normal: عرض كل المكونات
   - Error: رسالة خطأ لطيفة مع زر إعادة المحاولة

6. الأداء:
   - حساب النبض في requestIdleCallback أو setTimeout(0) لعدم حجب الـ UI
   - cache النتيجة في sessionStorage مع timestamp
   - إذا البيانات أقل من 5 دقائق → استخدم الـ cache

7. التصميم:
   - RTL بالكامل
   - خلفية الصفحة: تدرج خفيف من رمادي فاتح
   - مسافات متسقة بين المكونات (gap-4 أو gap-6)
   - scroll عمودي سلس
   - max-width: 640px في المنتصف (mobile-optimized)
```

---

## المرحلة 2: صندوق الوارد المالي (المستحقات)

### برومبت 2.1 — محرك المستحقات

```
أنت مهندس بيانات مالية.

السياق: صندوق الوارد المالي في تطبيق قيد العقار — يعرض كل ما يستحق الانتباه.

المطلوب: أنشئ `src/core/inbox-engine.js`

```js
export function calculateInbox(ledgerId) {
  return {
    // مستحقات متأخرة (فات موعدها)
    overdue: [
      {
        id, name, amount, type, // income | expense
        dueDate, daysOverdue,
        recurringItemId, // إذا مرتبط بالتزام متكرر
        priority: 'critical' | 'high' | 'medium',
      }
    ],
    
    // مستحقات هذا الأسبوع
    thisWeek: [ /* نفس الشكل */ ],
    
    // مستحقات هذا الشهر (بعد الأسبوع)
    thisMonth: [ /* نفس الشكل */ ],
    
    // ملخص
    summary: {
      totalOverdue: Number,
      totalOverdueAmount: Number,
      totalThisWeek: Number,
      totalThisWeekAmount: Number,
      totalThisMonth: Number,
      totalThisMonthAmount: Number,
    }
  };
}

// تحويل التزام متكرر إلى مستحقات
export function expandRecurringToDues(recurringItems, fromDate, toDate) {
  // لكل التزام متكرر:
  // - احسب مواعيد الاستحقاق بين fromDate و toDate
  // - تحقق أيها تم دفعه (عبر مطابقة الحركات)
  // - أرجع المستحقات غير المدفوعة
}
```

قواعد:
- استخدم الدوال الموجودة في ledger-store.js لقراءة الالتزامات المتكررة
- طابق المستحقات مع الحركات الفعلية لتحديد المدفوع
- التاريخ بتوقيت Asia/Riyadh
- ترتيب: الأقدم (الأكثر تأخرًا) أولاً في overdue، الأقرب أولاً في البقية
```

---

### برومبت 2.2 — واجهة صندوق الوارد

```
أنت مطور React ومصمم UX.

السياق: صفحة صندوق الوارد المالي في قيد العقار.

المطلوب: عدّل `src/pages/InboxPage.jsx`

التصميم:
┌─────────────────────────────────────────────┐
│ 📬 المستحقات                     [الدفتر ▾]│
│                                             │
│ ┌─ ⚠️ متأخرة (3) ─── 15,200 ر.س ────────┐ │
│ │                                         │ │
│ │ 🔴 إيجار شقة 5      5,000   متأخر 5 أيام│ │
│ │    □ [سجّل دفعة] [ذكّرني غدًا]          │ │
│ │                                         │ │
│ │ 🔴 فاتورة كهرباء    1,200   متأخر 2 يوم │ │
│ │    □ [سجّل دفعة]                        │ │
│ │                                         │ │
│ │ 🟡 عمولة مؤجلة      9,000   متأخر 1 يوم │ │
│ │    □ [سجّل دفعة]                        │ │
│ └─────────────────────────────────────────┘ │
│                                             │
│ ┌─ 📅 هذا الأسبوع (2) ── 7,500 ر.س ─────┐ │
│ │                                         │ │
│ │ ↗ إيجار فيلا 3       6,000   بعد يومين  │ │
│ │ ↙ رسوم تجديد          1,500   بعد 4 أيام│ │
│ └─────────────────────────────────────────┘ │
│                                             │
│ ┌─ 📆 هذا الشهر (5) ── 22,000 ر.س ──────┐ │
│ │  ... (مطوي افتراضيًا، اضغط للتوسيع)    │ │
│ └─────────────────────────────────────────┘ │
│                                             │
│ ────────────────────────────────────────     │
│ الإجمالي المتوقع هذا الشهر:                │
│ دخل: 35,000 ر.س | مصروف: 12,000 ر.س       │
└─────────────────────────────────────────────┘

متطلبات:

1. استخدم calculateInbox() من inbox-engine.js

2. أقسام مطوية/قابلة للتوسيع:
   - "متأخرة" مفتوحة دائمًا (إذا وُجدت)
   - "هذا الأسبوع" مفتوحة افتراضيًا
   - "هذا الشهر" مطوية افتراضيًا

3. لكل بند:
   - checkbox لتسجيل الدفعة بسرعة (يفتح modal مختصر)
   - زر "سجّل دفعة" → ينشئ حركة مالية مرتبطة بهذا البند
   - زر "ذكّرني غدًا" → يؤجل التنبيه (يحفظ في localStorage)

4. فلتر:
   - الكل / دخل فقط / مصروف فقط
   - تبديل بين الدفاتر

5. Empty state: "لا توجد مستحقات — استمتع بيومك! ☀️"

6. Pull-to-refresh + تحديث عند ledger:activeChanged
```

---

### برومبت 2.3 — Modal تسجيل الدفعة السريع

```
أنت مطور React.

المطلوب: أنشئ `src/ui/inbox/QuickPaymentModal.jsx`

هذا modal يظهر عند الضغط على "سجّل دفعة" من صندوق الوارد.

التصميم:
┌─────────────────────────────────────────────┐
│         تسجيل دفعة سريع                     │
│                                      [ ✕ ]  │
│                                             │
│  إيجار شقة 5                                │
│  المبلغ المستحق: 5,000 ر.س                  │
│                                             │
│  المبلغ المدفوع:  [  5,000  ] ر.س           │
│  (يمكن تعديله للدفع الجزئي)                 │
│                                             │
│  تاريخ الدفع:     [  اليوم  ▾]              │
│                                             │
│  ملاحظة (اختياري): [          ]              │
│                                             │
│  ┌─────────────────────────────────────┐    │
│  │         ✅ تسجيل الدفعة             │    │
│  └─────────────────────────────────────┘    │
│                                             │
│  أو: [ تأجيل لتاريخ آخر ]                  │
└─────────────────────────────────────────────┘

متطلبات:

1. يستقبل: dueItem (البند المستحق)، onConfirm callback، onClose

2. المبلغ: محدد مسبقًا بالمبلغ المستحق، قابل للتعديل (دفع جزئي)

3. عند التأكيد:
   - ينشئ حركة مالية عبر dataStore.addTransaction()
   - يربطها بالدفتر النشط (ledgerId)
   - يربطها بالالتزام المتكرر (recurringItemId) إن وُجد
   - يغلق الـ modal
   - يُطلق حدث لتحديث صندوق الوارد والنبض

4. حالة الإرسال: زر loading → نجاح (✅ مع إغلاق تلقائي بعد 1.5 ثانية)

5. RTL + تنسيق أرقام عربي
```

---

## المرحلة 3: التنبيهات الاستباقية

### برومبت 3.1 — محرك التنبيهات الذكية

```
أنت مهندس أنظمة ذكية.

السياق: تطبيق قيد العقار — نظام التنبيهات الاستباقية.

المطلوب: أنشئ `src/core/alert-engine.js`

```js
/**
 * محرك التنبيهات الذكية
 * يحلل البيانات ويولّد تنبيهات استباقية
 */

export function generateSmartAlerts(ledgerId) {
  return detectAllAlerts(ledgerId).sort(bySeverityThenDate);
}

// === أنواع التنبيهات ===

// 1. أزمة سيولة متوقعة
function detectCashflowCrisis(ledgerId) {
  // المنطق:
  // - احسب الرصيد الحالي
  // - أضف الدخل المتوقع من الالتزامات المتكررة (30 يوم قادمة)
  // - اطرح المصروفات المتوقعة
  // - إذا الرصيد سيصل تحت الصفر → تنبيه
  // - حدد التاريخ المتوقع للأزمة والمبلغ المطلوب
  // 
  // الرسالة: "تحذير: في {التاريخ} ستحتاج {المبلغ} ر.س إضافية لتغطية التزاماتك"
}

// 2. نمط إنفاق غير طبيعي
function detectSpendingAnomaly(ledgerId) {
  // المنطق:
  // - احسب متوسط المصروفات اليومية لآخر 30 يوم
  // - إذا مصروف اليوم > 3 × المتوسط → تنبيه
  //
  // الرسالة: "مصروفات اليوم ({المبلغ}) أعلى بكثير من المعتاد ({المتوسط})"
}

// 3. دخل متوقع لم يصل
function detectMissedIncome(ledgerId) {
  // المنطق:
  // - راجع الالتزامات المتكررة من نوع "دخل"
  // - إذا فات موعد الاستحقاق ولا توجد حركة دخل مطابقة → تنبيه
  //
  // الرسالة: "{اسم البند} كان يفترض وصوله في {التاريخ} ولم يُسجل بعد"
}

// 4. تحسن/تدهور الصحة المالية
function detectHealthTrend(ledgerId) {
  // المنطق:
  // - قارن healthScore هذا الأسبوع بالأسبوع الماضي
  // - إذا انخفض > 10 نقاط → تنبيه تدهور
  // - إذا ارتفع > 10 نقاط → تنبيه إيجابي (تحفيزي)
}

// 5. التزام بدون حركات منذ فترة
function detectDormantCommitments(ledgerId) {
  // المنطق:
  // - إذا التزام متكرر لم يُسجل له أي حركة منذ دورتين → تنبيه
  // - قد يعني أن البند لم يعد فعالًا أو أن المستخدم نسي التسجيل
}
```

قواعد:
- كل تنبيه يتبع نفس الشكل المعرّف في pulse-engine (type, severity, title, amount, etc.)
- لا تكرر التنبيهات — إذا نفس التنبيه ظهر مسبقًا ولم يُتعامل معه، لا تولده مرة أخرى
- خزّن التنبيهات المُرفضة (dismissed) في localStorage بـ TTL (7 أيام)
- أقصى 10 تنبيهات في المرة الواحدة
```

---

### برومبت 3.2 — مركز التنبيهات في الواجهة

```
أنت مطور React.

المطلوب: أنشئ `src/ui/alerts/AlertCenter.jsx`

هذا مكون يظهر كـ notification badge في شريط التنقل + panel منبثق عند الضغط.

1. Badge:
   - رقم أحمر صغير فوق أيقونة الجرس
   - يرتجف (pulse animation) إذا يوجد تنبيه critical

2. Panel (عند الضغط):
   - ينزلق من الأعلى (slide-down)
   - قائمة التنبيهات مع:
     * أيقونة حسب النوع
     * العنوان والوصف
     * زمن التنبيه (منذ X ساعات)
     * أزرار: [اتخذ إجراء] [رفض] [تأجيل]
   - زر "مسح الكل" في الأسفل

3. التنبيه الحرج يظهر كـ banner ثابت أعلى الشاشة (sticky):
   - لا يختفي حتى يُتعامل معه
   - خلفية حمراء مع نص أبيض
```

---

### برومبت 3.3 — نظام تأجيل وأرشفة التنبيهات

```
أنت مطور React + منطق أعمال.

المطلوب: أنشئ `src/core/alert-manager.js`

```js
const STORAGE_KEY = 'ff_alert_state';

export const AlertManager = {
  // رفض تنبيه (لا يظهر مجددًا لمدة 7 أيام)
  dismiss(alertId) {},
  
  // تأجيل تنبيه (يظهر مجددًا بعد X ساعات)
  snooze(alertId, hours = 24) {},
  
  // هل التنبيه مخفي (مرفوض أو مؤجل)؟
  isHidden(alertId) {},
  
  // تنظيف المنتهية
  cleanup() {},
  
  // إحصائيات (للذكاء)
  getStats() {
    return {
      totalDismissed: Number,
      totalSnoozed: Number,
      mostDismissedType: String,  // أكثر نوع يرفضه المستخدم
    };
  }
};
```
```

---

## المرحلة 4: مقارنة الدفاتر

### برومبت 4.1 — محرك المقارنة

```
أنت مهندس بيانات مالية.

المطلوب: أنشئ `src/core/ledger-compare.js`

```js
export function compareLedgers(ledgerIds) {
  // يقارن 2-5 دفاتر ويرجع:
  return {
    ledgers: [
      {
        id, name, type,
        healthScore: Number,
        totalIncome30d: Number,      // دخل آخر 30 يوم
        totalExpense30d: Number,     // مصروف آخر 30 يوم
        netCashflow30d: Number,      // صافي
        overdueCount: Number,        // عدد المتأخرات
        overdueAmount: Number,       // مبلغ المتأخرات
        roi: Number,                 // عائد الاستثمار (دخل/مصروف)
        trend: 'improving' | 'declining' | 'stable',
      }
    ],
    
    // أفضل وأسوأ
    bestPerformer: { ledgerId, reason },     // مثل: "أعلى ROI"
    worstPerformer: { ledgerId, reason },    // مثل: "أكثر متأخرات"
    
    // توصيات
    recommendations: [
      { message: "دفتر X يستنزف أكثر مما ينتج — راجع التزاماته" }
    ]
  };
}
```
```

---

### برومبت 4.2 — واجهة المقارنة

```
أنت مطور React ومصمم بيانات مرئية.

المطلوب: أنشئ `src/ui/ledger/LedgerCompare.jsx`

التصميم:
┌─────────────────────────────────────────────┐
│ 📊 مقارنة الدفاتر         [اختر الدفاتر ▾] │
│                                             │
│  ┌────────────┬────────────┬────────────┐   │
│  │  مكتب قيد  │  فيلا 3    │  شقة 5     │   │
│  │            │            │            │   │
│  │  صحة: 82   │  صحة: 65   │  صحة: 41   │   │
│  │  ██████░░  │  █████░░░  │  ████░░░░  │   │
│  │            │            │            │   │
│  │ دخل: 25K  │ دخل: 8K   │ دخل: 5K   │   │
│  │ صرف: 12K  │ صرف: 6K   │ صرف: 7K   │   │
│  │ صافي:+13K │ صافي:+2K  │ صافي:-2K  │   │
│  │            │            │            │   │
│  │ ROI: 2.1x │ ROI: 1.3x │ ROI: 0.7x │   │
│  │            │            │            │   │
│  │  ⭐ الأفضل │            │ ⚠️ يحتاج   │   │
│  │           │            │   مراجعة   │   │
│  └────────────┴────────────┴────────────┘   │
│                                             │
│ 💡 توصية: شقة 5 تستنزف أكثر مما تنتج.     │
│    راجع التزاماتها أو فكّر في تعديل الإيجار │
└─────────────────────────────────────────────┘

متطلبات:
- بطاقات متجاورة (horizontal scroll على الموبايل)
- أشرطة تقدم للصحة بألوان ديناميكية
- ROI: أخضر إذا > 1، أحمر إذا < 1
- التوصيات في الأسفل بخلفية صفراء فاتحة
- RTL + أرقام عربية
- اختيار الدفاتر بـ multi-select dropdown
```

---

## المرحلة 5: التقرير الشهري PDF

### برومبت 5.1 — مولّد التقرير

```
أنت مهندس تقارير مالية.

المطلوب: أنشئ `src/core/monthly-report-generator.js`

```js
export async function generateMonthlyReport(ledgerId, month, year) {
  // يجمع كل بيانات الشهر ويولد كائن تقرير:
  return {
    meta: { ledgerName, ledgerType, month, year, generatedAt },
    
    summary: {
      openingBalance: Number,
      closingBalance: Number,
      totalIncome: Number,
      totalExpense: Number,
      netCashflow: Number,
      healthScore: Number,
      healthTrend: String,        // مقارنة بالشهر السابق
    },
    
    // تفصيل الدخل حسب المصدر
    incomeBreakdown: [
      { source: String, amount: Number, percentage: Number, count: Number }
    ],
    
    // تفصيل المصروفات حسب الفئة
    expenseBreakdown: [
      { category: String, amount: Number, percentage: Number, count: Number }
    ],
    
    // الالتزامات
    commitments: {
      totalDue: Number,
      totalPaid: Number,
      totalOverdue: Number,
      complianceRate: Number,     // نسبة الالتزام بالمواعيد
    },
    
    // أبرز الأحداث
    highlights: [
      { type: 'positive' | 'negative' | 'neutral', message: String }
    ],
    
    // التوقعات للشهر القادم
    nextMonthForecast: {
      expectedIncome: Number,
      expectedExpense: Number,
      expectedNet: Number,
      risks: [String],
    },
    
    // قائمة الحركات التفصيلية
    transactions: [ /* كل حركات الشهر */ ],
  };
}
```
```

---

### برومبت 5.2 — واجهة التقرير + تصدير PDF

```
أنت مطور React + مهندس PDF.

المطلوب: أنشئ:
1. `src/ui/reports/MonthlyReportView.jsx` — عرض التقرير في المتصفح
2. `src/utils/report-to-pdf.js` — تحويل التقرير إلى PDF

لعرض التقرير في المتصفح:
- تصميم صفحة A4 (عمودي) بخلفية بيضاء
- شعار "قيد العقار" في الأعلى
- اسم الدفتر + الشهر/السنة
- كل قسم من أقسام التقرير بتصميم واضح:
  * ملخص: بطاقة كبيرة بالأرقام الرئيسية
  * تفصيل الدخل: جدول + pie chart (SVG)
  * تفصيل المصروفات: جدول + pie chart
  * الالتزامات: شريط تقدم (نسبة الالتزام)
  * أبرز الأحداث: قائمة ملونة
  * التوقعات: بطاقة مع مؤشرات

لتصدير PDF:
- استخدم html2canvas + jsPDF (أو أي بديل خفيف)
- أو استخدم window.print() مع @media print CSS
- التقرير يجب أن يكون قابلًا للقراءة عند الطباعة
- أضف زر "تصدير PDF" و "مشاركة" (Web Share API)
- اسم الملف: "تقرير_قيد_{اسم_الدفتر}_{الشهر}_{السنة}.pdf"

RTL + خطوط عربية واضحة في الـ PDF
```

---

## المرحلة 6: مؤشر صحة المكتب

### برومبت 6.1 — خوارزمية مؤشر الصحة المتقدم

```
أنت عالم بيانات مالية.

السياق: نبني "مؤشر قيد" — درجة صحة مالية من 0 إلى 100 للمكتب العقاري.

المطلوب: أنشئ `src/core/health-score-algorithm.js`

```js
/**
 * خوارزمية مؤشر الصحة المالية "مؤشر قيد"
 * 
 * الدرجة النهائية = مجموع مرجح لـ 5 محاور:
 * 
 * 1. الالتزام بالمواعيد (30%):
 *    - نسبة الالتزامات المدفوعة في وقتها خلال 90 يوم
 *    - 100% في الوقت = 30 نقطة
 *    - كل 10% تأخير = -3 نقاط
 * 
 * 2. صحة التدفق النقدي (25%):
 *    - هل الدخل يغطي المصروفات باستمرار؟
 *    - نسبة الأشهر الموجبة من آخر 6 أشهر
 *    - 6/6 أشهر موجبة = 25 نقطة
 *    - كل شهر سالب = -4 نقاط
 * 
 * 3. استقرار الدخل (20%):
 *    - معامل الاختلاف (CV) للدخل الشهري
 *    - CV < 0.1 (مستقر جدًا) = 20 نقطة
 *    - CV > 0.5 (غير مستقر) = 5 نقاط
 * 
 * 4. كفاية الاحتياطي (15%):
 *    - هل الرصيد الحالي يغطي مصروفات X أشهر؟
 *    - > 3 أشهر = 15 نقطة
 *    - 1-3 أشهر = 10 نقاط
 *    - < 1 شهر = 3 نقاط
 * 
 * 5. اتجاه الأداء (10%):
 *    - مقارنة آخر 3 أشهر بالـ 3 السابقة
 *    - تحسن > 10% = 10 نقاط
 *    - مستقر = 7 نقاط
 *    - تدهور > 10% = 3 نقاط
 */

export function calculateHealthScore(ledgerId) {
  return {
    totalScore: Number,         // 0-100
    
    breakdown: {
      commitmentScore: { score: Number, max: 30, details: String },
      cashflowScore: { score: Number, max: 25, details: String },
      stabilityScore: { score: Number, max: 20, details: String },
      reserveScore: { score: Number, max: 15, details: String },
      trendScore: { score: Number, max: 10, details: String },
    },
    
    grade: 'A+' | 'A' | 'B+' | 'B' | 'C+' | 'C' | 'D' | 'F',
    gradeLabel: String,         // مثل: "ممتاز", "جيد", "يحتاج تحسين", "حرج"
    
    // أهم 3 نصائح لتحسين الدرجة
    improvements: [
      {
        axis: String,           // أي محور
        currentScore: Number,
        potentialGain: Number,  // كم نقطة ممكن تكسب
        advice: String,         // مثل: "سدد 3 التزامات متأخرة لتكسب 9 نقاط"
      }
    ],
    
    // مقارنة مع الشهر السابق
    previousScore: Number,
    changeDirection: 'up' | 'down' | 'stable',
    changeAmount: Number,
  };
}
```
```

---

### برومبت 6.2 — واجهة مؤشر الصحة (Health Dashboard)

```
أنت مطور React ومصمم واجهات مالية احترافية.

المطلوب: أنشئ `src/ui/health/HealthDashboard.jsx`

التصميم:
┌─────────────────────────────────────────────┐
│              مؤشر قيد                       │
│          صحة مكتبك المالية                  │
│                                             │
│          ╭─────────────────╮                │
│          │                 │                │
│          │      B+         │   ← التقدير    │
│          │     جيد جدًا    │                │
│          │                 │                │
│          │      73/100     │   ← الدرجة     │
│          │    ↑ +5 نقاط    │   ← التغيير    │
│          ╰─────────────────╯                │
│                                             │
│  ┌──────────────────────────────────────┐   │
│  │ الالتزام بالمواعيد   ████████░░ 24/30│   │
│  │ صحة التدفق          ██████░░░░ 18/25│   │
│  │ استقرار الدخل       █████████░ 17/20│   │
│  │ كفاية الاحتياطي     ██████░░░░ 8/15 │   │
│  │ اتجاه الأداء        ██████░░░░ 6/10 │   │
│  └──────────────────────────────────────┘   │
│                                             │
│  💡 كيف تحسّن درجتك؟                       │
│  ┌──────────────────────────────────────┐   │
│  │ 1. سدد 3 التزامات متأخرة → +9 نقاط │   │
│  │ 2. حافظ على رصيد شهرين → +7 نقاط   │   │
│  │ 3. سجّل الدخل المتأخر → +4 نقاط    │   │
│  └──────────────────────────────────────┘   │
│                                             │
│  الدرجة المتوقعة بعد التحسين: 93 (A+)      │
└─────────────────────────────────────────────┘

متطلبات:
- الدائرة الكبيرة: SVG مع animation
- أشرطة التقدم: ألوان ديناميكية حسب النسبة
- نصائح التحسين: مرتبة من الأعلى تأثيرًا
- "الدرجة المتوقعة": تحسب إذا المستخدم نفذ كل النصائح
- RTL + تصميم Mobile-first
```

---

### برومبت 6.3 — تاريخ مؤشر الصحة

```
أنت مطور React.

المطلوب: أنشئ `src/core/health-history.js` + `src/ui/health/HealthTrend.jsx`

health-history.js:
- يحفظ درجة الصحة يوميًا في localStorage (مفتاح: ff_health_history)
- يحفظ آخر 90 يوم فقط (ينظف القديم)
- شكل التخزين: { ledgerId: [{ date, score, grade }] }

HealthTrend.jsx:
- رسم بياني خطي (SVG) يعرض تطور الدرجة على مدار 30/60/90 يوم
- خط أفقي عند 70 (الحد الأدنى "الجيد")
- نقاط ملونة: أخضر فوق 70، أحمر تحت 40
- تبديل الفترة: 30 يوم / 60 يوم / 90 يوم
- عرض أعلى وأدنى درجة مع تواريخها
```

---

## المرحلة 7: التنظيف النهائي والتلميع

### برومبت 7.1 — توحيد التصميم + Dark Mode Ready

```
أنت مصمم أنظمة تصميم (Design System Engineer).

السياق: أنهينا بناء كل مكونات النبض المالي لتطبيق قيد العقار.

المطلوب: أنشئ `src/styles/pulse-theme.js` + حدّث Tailwind config

1. نظام ألوان موحد:
```js
export const PULSE_COLORS = {
  health: {
    excellent: { bg: '#ECFDF5', text: '#065F46', accent: '#10B981' },
    good:      { bg: '#EFF6FF', text: '#1E40AF', accent: '#3B82F6' },
    warning:   { bg: '#FFFBEB', text: '#92400E', accent: '#F59E0B' },
    critical:  { bg: '#FEF2F2', text: '#991B1B', accent: '#EF4444' },
  },
  transaction: {
    income:  '#10B981',
    expense: '#EF4444',
    neutral: '#6B7280',
  },
  ui: {
    primary: '#1E3A5F',      // أزرق داكن (هوية قيد)
    secondary: '#64748B',
    background: '#F8FAFC',
    card: '#FFFFFF',
    border: '#E2E8F0',
  }
};
```

2. نظام خطوط:
   - العناوين: 'IBM Plex Sans Arabic' أو 'Tajawal'
   - الأرقام: 'Tabular Nums' (monospace للأرقام المالية)
   - حجم الرقم الكبير في النبض: 48px
   - حجم العنوان: 24px
   - حجم النص: 16px
   - حجم التفاصيل: 14px

3. أنماط مشتركة (utility classes):
   - .pulse-card: rounded-2xl shadow-sm border p-6
   - .pulse-amount: font-tabular text-lg font-semibold
   - .pulse-label: text-sm text-gray-500
   - .pulse-badge: rounded-full px-3 py-1 text-xs font-medium

4. Animations:
   - fadeIn, slideUp, countUp, pulse, shake
   - كلها بـ CSS @keyframes (بدون مكتبة)

5. Dark Mode Variables (جهّزها بدون تفعيلها):
   - prefers-color-scheme: dark متغيرات جاهزة
```

---

### برومبت 7.2 — Onboarding وأول استخدام

```
أنت مصمم UX متخصص في تجربة المستخدم الأولى (First-Time User Experience).

المطلوب: أنشئ `src/ui/onboarding/PulseOnboarding.jsx`

تجربة المستخدم الجديد (يظهر مرة واحدة):

الخطوة 1: الترحيب
┌─────────────────────────────────────────────┐
│                                             │
│          ⭐ مرحبًا بك في قيد                │
│                                             │
│    اعرف صحة مكتبك المالية بثانية واحدة     │
│                                             │
│        [ابدأ الإعداد — دقيقة واحدة]         │
└─────────────────────────────────────────────┘

الخطوة 2: إنشاء أول دفتر
┌─────────────────────────────────────────────┐
│                                             │
│     ما نوع العقار أو المكتب الذي تديره؟    │
│                                             │
│  [🏢 مكتب عقاري]  [🏠 شقة]  [🏡 فيلا]     │
│  [🏗 مبنى]  [🏖 شاليه]  [📋 أخرى]          │
│                                             │
│  اسم الدفتر: [                    ]         │
│                                             │
│              [التالي →]                      │
└─────────────────────────────────────────────┘

الخطوة 3: إضافة أول التزام
┌─────────────────────────────────────────────┐
│                                             │
│    أضف أول التزام متكرر (اختياري)           │
│                                             │
│  القوالب الجاهزة:                           │
│  [✅ إيجار شهري]   [فاتورة كهرباء]          │
│  [صيانة دورية]    [عمولة وسيط]             │
│                                             │
│  أو: [إضافة التزام مخصص]                    │
│                                             │
│    [التالي →]     [تخطي]                    │
└─────────────────────────────────────────────┘

الخطوة 4: النبض جاهز!
┌─────────────────────────────────────────────┐
│                                             │
│           🎉 تم الإعداد!                    │
│                                             │
│    نبضك المالي جاهز. أضف حركات مالية       │
│    وسيتحسن مؤشر الصحة مع كل تسجيل.        │
│                                             │
│         [افتح النبض المالي]                  │
└─────────────────────────────────────────────┘

متطلبات:
- حفظ حالة الـ onboarding في localStorage (ff_onboarding_complete)
- الخطوات قابلة للتخطي
- تصميم هادئ ونظيف بدون إرهاق بصري
- RTL بالكامل
- حركات انتقال سلسة بين الخطوات
- أنشئ الدفتر والالتزامات فعليًا عبر ledger-store و dataStore
```

---

## 📌 ملاحظات تنفيذية مهمة

### ترتيب التنفيذ الصارم:
```
المرحلة 0 (كلها) → 1.1 → 0.4 → 1.2 → 1.3 → 1.4 → 1.5 →
2.1 → 2.2 → 2.3 → 3.1 → 3.2 → 3.3 →
4.1 → 4.2 → 5.1 → 5.2 → 6.1 → 6.2 → 6.3 →
7.1 → 7.2
```

### عند كل برومبت أرفق:
1. هذا الملف (كسياق عام)
2. الملف/الملفات المشار إليها في البرومبت
3. أي ملف أنشأته في برومبت سابق ويعتمد عليه البرومبت الحالي

### اختبار بعد كل مرحلة:
- هل التطبيق يعمل بدون أخطاء؟
- هل الصفحة الرئيسية تعرض النبض؟
- هل البيانات تتحدث عند تغيير الدفتر النشط؟
- هل الواجهة RTL بالكامل؟
- هل تعمل على الموبايل؟

---

*تم إعداد هذه البرومبتات لمشروع قيد العقار — إعادة تغليف الدفاتر كـ "النبض المالي"*
*"افتح قيد كل صباح — واعرف صحة مكتبك المالية بثانية واحدة"*




